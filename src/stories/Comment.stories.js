import { Comment } from "../components/Comment";

export default {
  title: "Comment",
  component: Comment,

  parameters: {
    layout: "centered",
  },
};

export const Default = {
  args: {
    variant: "default",
  },
};
