import { Input } from "../components/Input";

export default {
  title: "Input",
  component: Input,

  parameters: {
    layout: "centered",
  },
};

export const Default = {
  args: {
    variant: "default",
  },
};
